# Pydantic AI + Modal Harness
# Install: pip install -r requirements.txt

# Orchestrator (Pydantic AI with the Anthropic provider)
pydantic-ai-slim[anthropic]>=2.38.0

# Long-running compute (CPU / GPU / Sandbox)
modal>=1.5.0

# API Server + Server-Sent Events
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.0.0

# HTTP client (webhooks, callbacks)
httpx>=0.27.0

# Numeric workloads (torch is only installed inside the Modal GPU image)
numpy>=1.26.0

# Config / utils
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Observability - Phoenix (optional, enable with PHOENIX_ENABLED=true)
arize-phoenix-otel>=0.14.0
opentelemetry-sdk>=1.28.0
opentelemetry-exporter-otlp>=1.28.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
